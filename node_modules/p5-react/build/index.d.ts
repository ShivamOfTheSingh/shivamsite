import React from "react";
import P5 from "p5";
declare global {
    interface Window {
        p5: P5;
    }
}
type SketchI = {
    sketch: (...args: any[]) => any;
    setup: (p5: P5, containerRef: HTMLDivElement) => void;
} & React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement>;
declare const Sketch: React.MemoExoticComponent<({ sketch: propSketch, setup, ...props }: SketchI) => React.JSX.Element>;
export default Sketch;
